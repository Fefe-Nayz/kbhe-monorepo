# Proto MCU V1

First MCU prototype archived from the downloaded `hall-effect-keyboard-04f0a440...` source tree.

The project is self-contained: footprints, symbols, 3D models, and datasheets are stored locally under `Library/` and `Docs/`.
