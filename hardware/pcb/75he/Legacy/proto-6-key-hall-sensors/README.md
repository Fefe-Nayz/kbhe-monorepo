# Proto 6-Key Hall Sensors

Six-key prototype used to validate the hall-effect sensing approach only.

This board has no MCU and no keyboard logic. Its local `Library/` and `Docs/` folders are intentionally vendored so the project can be opened independently from the root PCB.
