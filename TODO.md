The info chips should have rounded corners.
The analog curve should have a list of presets and we should enable an option to see where we are on this curve right now.
In the joystick live preview, the little progress bar under the joystick, on light theme the background should adapt accrodingly
The global response layout is a bit wierd (same issue as the other layout issue)
The per key mappinf in the table is good but the table height should be more and the lines must have a bigger height
The lightning matric page layout still have some issues:
the example colors buttons should all be the same width (not based on string length)
The matrix is a bit small and a bit cropped on the right and bottom, it should fill the width (while maintaiing aspect ratio)
Same remarks about color buttons in the effects page, also make sure that the effect mode static/utility, ambiant motion and reactive sections still allow only one selected across all sections instead this is a little wierd
A recent change made the debug/sensors page extremely laggy when polling is enabled even at low update rate. This was not at all an issue before and is not on other pages relying on polling
The overview page looks not very good in my opinion, for an overview page it should be a bit more fancy, also on app boot, it's data is not fetched fo the overview page.