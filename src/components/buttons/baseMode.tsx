import { Button } from "@/components/ui/button";

export default function ToogleBaseMode() {
  return (
    <>
      <Button className="bg-primary text-white text-xs">
        Show Keymap
      </Button>
    </>
  );
}